apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "saasbuilder.fullname" . }}-config
  labels:
    {{- include "saasbuilder.labels" . | nindent 4 }}
data:
  {{- range $k, $v := .Values.env }}
  {{ $k }}: {{ $v | quote }}
  {{- end }}
