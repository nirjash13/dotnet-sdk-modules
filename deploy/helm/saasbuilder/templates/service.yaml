apiVersion: v1
kind: Service
metadata:
  name: {{ include "saasbuilder.fullname" . }}
  labels:
    {{- include "saasbuilder.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.targetPort }}
      protocol: TCP
      name: http
  selector:
    {{- include "saasbuilder.selectorLabels" . | nindent 4 }}
