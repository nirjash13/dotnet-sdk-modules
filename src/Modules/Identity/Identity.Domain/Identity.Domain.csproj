<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>
    <TargetFramework>net10.0</TargetFramework>
    <IsPackable>true</IsPackable>
    <PackageId>SaasBuilder.Modules.Identity.Domain</PackageId>
    <Title>SaasBuilder Identity Domain</Title>
    <Description>Domain model for the Identity module. Zero infrastructure dependencies.</Description>
    <Authors>SaasBuilder Contributors</Authors>
    <PackageLicenseExpression>MIT</PackageLicenseExpression>
    <RepositoryUrl>https://github.com/placeholder/modular-sdk-dotnet</RepositoryUrl>
    <RepositoryType>git</RepositoryType>
    <PackageTags>saasbuilder;identity;domain;organizations;rbac</PackageTags>
  </PropertyGroup>

  <!--
    Domain layer must have ZERO dependencies on EF Core / ASP.NET Core / MassTransit /
    OpenIddict / FluentValidation. NetArchTest enforces this in Phase 9.
    Only Microsoft.Extensions.* abstractions are permitted via transitive SharedKernel reference.
  -->

  <ItemGroup>
    <ProjectReference Include="..\..\..\SaasBuilder.SharedKernel\SaasBuilder.SharedKernel.csproj" />
  </ItemGroup>

  <ItemGroup Label="Analyzers — build-time only">
    <PackageReference Include="Microsoft.CodeAnalysis.NetAnalyzers" PrivateAssets="All" />
    <PackageReference Include="Roslynator.Analyzers" PrivateAssets="All" />
    <PackageReference Include="StyleCop.Analyzers" PrivateAssets="All" />
  </ItemGroup>

  <ItemGroup Label="StyleCop configuration">
    <AdditionalFiles Include="$(MSBuildThisFileDirectory)..\..\..\..\stylecop.json" />
  </ItemGroup>

  <PropertyGroup>
    <NoWarn>$(NoWarn);SA1101;SA1309;SA0001;SA1600;SA1200</NoWarn>
  </PropertyGroup>

</Project>
