using System.Net;
using System.Net.Http.Json;
using FluentAssertions;
using Xunit;

namespace __ModuleName__.Tests;

/// <summary>Integration tests for <see cref="__FeatureName__"/> feature.</summary>
public sealed class __FeatureName__Tests
{
    [Fact]
    public async Task __FeatureName___WhenRequestIsValid_ReturnsSuccess()
    {
        // TODO: use WebApplicationFactory + real DB (TestContainers) for integration test
        // Arrange
        // Act
        // Assert
        await Task.CompletedTask.ConfigureAwait(false);
    }
}
